__pycache__/
*.pyc
.cache/
users.json
settings.json
# A real browser session's cookies for forexfactory.com (see
# scripts/fetch_forexfactory.py) -- never commit this, it's a live security
# token that would let anyone with the repo act as that logged-in session.
forexfactory_cookies.json
